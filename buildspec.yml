version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
      
  build:
    commands:
      - npm run build
      - echo "Build completed at $(date)"

artifacts:
  files:
    - dist/**/*
  base-directory: dist

cache:
  paths:
    - node_modules/**/*
